services:
  postgres:
    image: postgres:16
    container_name: guardai-postgres
    environment:
      POSTGRES_USER: guardai
      POSTGRES_PASSWORD: guardai
      POSTGRES_DB: guardai
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
